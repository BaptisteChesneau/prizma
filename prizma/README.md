# Hello Flask

Un petit Hello World Flask.

## Lancer
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python -m flask --app app run --debug
